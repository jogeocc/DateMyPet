<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jgchan.datemypet">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".RegistroUsuarioActivity"
            android:label="Datos personales" />
        <activity android:name=".RegistroActivity" />
        <activity
            android:name=".VeterinarioActivity"
            android:label="@string/title_activity_veterinario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistroCitasActivity"
            android:label="@string/title_activity_registro_citas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistroVacunasActivity"
            android:label="@string/title_activity_registro_vacunas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".IngresarActivity" />
        <activity
            android:name=".MenuActivity"
            android:label="Inicio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VerPerfilActivity"
            android:label="Mi perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditarPerfilActivity"
            android:label="@string/title_activity_editar_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListaMascotasActivity"
            android:label="@string/title_activity_lista_mascotas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".adaptadores.verImgMascotaDialog"
            android:theme="@android:style/Theme.Material.Dialog.NoActionBar"
            tools:targetApi="lollipop" />
        <activity
            android:name=".InfoMascotaActivity"
            android:label="@string/title_activity_info_mascota"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>